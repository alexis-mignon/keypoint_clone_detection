# keypoint_clone_detection

Simple clone detection based on keypoint detection.

```bash
$ python keypoint_clone_detection.py input_image output_image
```

Can get help using:
```bash
$ python keypoint_clone_detection.py --help
```
